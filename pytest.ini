[pytest]
minversion = 6.0

# Default to fast, deterministic unit tests. Playwright tests are run explicitly
# via their own config file under tests/playwright/.
testpaths = tests/unit
norecursedirs = tests/playwright

addopts =
    -ra
    --strict-config
    --strict-markers

